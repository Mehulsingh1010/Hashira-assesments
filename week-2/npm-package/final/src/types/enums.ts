export enum FieldStatus {
  Valid = "VALID",
  Invalid = "INVALID",
  Untouched = "UNTOUCHED",
}

export enum SubmissionState {
  Idle = "IDLE",
  Submitting = "SUBMITTING",
  Submitted = "SUBMITTED",
}
